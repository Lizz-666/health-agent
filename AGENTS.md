# Project Agent Governance

> 适用范围：本仓库及其所有 Git worktree。最后审查：2026-07-16。
> OpenCode 会把根目录 `AGENTS.md` 注入所选模型的上下文；模型是 Claude、GPT 或其他提供者时规则相同。必须从目标 worktree 根目录或其子目录启动 OpenCode。

## 1. 项目边界

本项目面向一般健康成年人，提供健康科普、体态筛查辅助、普通健身与饮食建议和执行管理。它不是医疗诊断、治疗、专业康复、营养治疗或专业教练替代系统。

规范来源：

- `docs/product/vision.md`
- `docs/product/safety-boundaries.md`
- `docs/product/roadmap.md`
- 当前已批准的 ADR、规格和实施计划
- `docs/agent/ACTIVE_TASKS.md` 中登记的当前任务

旧计划、旧规格、`issue.md` 和聊天记录只作为历史材料，不能直接成为当前产品事实、安全规则或完成状态。

## 2. 上下文路由

每个开发会话先读取本文件，再运行 `git status --short --branch` 和 `git rev-parse HEAD`。

实现 Agent 只读取完成当前 Task 所需的上下文：Task 段落、Task 明确链接的规格章节、受影响代码和测试。不要默认通读整个路线图、整份大规格或整份实施计划。

以下工作需要扩大读取范围：

- 产品范围、跨模块架构或阶段规划：读取愿景、路线图和相关架构文档
- 健康建议、风险规则、隐私、同意、照片、健康档案或产品运行时 Agent：读取安全边界和相关当前规格
- 迁移、公共 API 或跨端契约：读取相关模型、迁移、OpenAPI、Flutter 调用方和测试

找不到明确的当前 Task、规格章节或文件范围时，不开始实现，先报告缺口。

## 3. 角色与权限

### 协调者（当前为 Codex）

负责维护边界、规格、计划和任务总账；拆分任务；编写提示词；检查真实 diff；做规格符合性与工程质量/安全两轮审查；重新运行关键验证；判断技术上是否可合并；维护 worktree 和集成顺序。

用户已授予 Codex 持续授权：满足以下全部条件时，可以自动创建本地 commit，无需逐次确认：

1. Codex 已独立检查真实 diff，而非只依赖实现报告。
2. 任务验收和风险匹配的验证已在当前内容上重新运行并通过。
3. 没有未解决的 P0/P1/P2 finding；剩余风险已明确记录。
4. staging 可以精确限制为已审查文件，不会混入用户或其他 Agent 的改动。
5. commit 不包含密钥、真实健康数据、运行产物或未审查的生成文件。

该授权只覆盖本地 commit，不覆盖 merge、rebase、push、PR、发布、外部写入、真实数据处理或不可逆操作。上述操作仍需用户明确授权。无法精确隔离提交范围时不得自动提交。

### 实现 Agent（OpenCode + 所选模型）

只执行登记的单个 Task；遵守文件范围和契约；对行为修复优先添加失败测试；运行全部要求的验证；报告真实结果。发现规格冲突、不安全要求或必须修改范围外文件时暂停并报告，不自行改写产品边界。

实现 Agent 不提交、合并、rebase 或 push，除非当前任务提示词明确授权。完成报告至少包含：base/head SHA、修改文件、行为变化、验证命令与退出结果、剩余风险、`git diff --stat` 和意外生成文件。

### 用户

决定产品取舍、体验偏好和高风险范围变化；授权 merge/push/发布、外部成本、真实数据处理和不可逆迁移；决定是否接受明确披露的残余风险。

## 4. 任务状态与完成定义

统一状态：

```text
planned -> in_progress -> review -> fixes -> verified -> committed -> merged
                                  \-> blocked
```

- 实现报告不等于 `verified`
- 验收通过不等于 `committed`
- 本地 commit 不等于 `merged`
- Task 合并不等于阶段完成
- 阶段完成必须满足路线图退出标准并有新鲜证据

每个 Task 必须定义目标、非目标、允许文件、禁止范围、契约、安全规则、依赖、验收条件、验证命令和报告格式。

## 5. 并行开发

- 每个实现 Task 使用独立分支和独立 worktree，禁止多个 Agent 在同一工作目录工作
- 开始前在 `docs/agent/ACTIVE_TASKS.md` 登记 Task、负责人、分支、worktree、base SHA、依赖、允许文件和状态
- 并行 Task 的写入文件不得重叠；确需重叠时按依赖顺序串行，不靠并行 rebase 解决
- 实现分支不得直接吸收其他未验收 Task 的改动
- 集成分支只接收已验证 commit，并按依赖顺序集成
- rebase、冲突修复、依赖变化或集成后，旧测试证据失效；必须在新 SHA 上重新验证
- worktree 出现 Task 范围外变化时暂停并报告，不清理、不覆盖、不顺手提交

建议命名：

```powershell
git worktree add ..\health-worktrees\<task-name> -b codex/<task-name> <base-sha>
```

## 6. Git 与变更控制

- 默认分支前缀为 `codex/`；一个 Task 使用一个或少量内聚 commit
- commit 前检查 `git status --short`、staged diff、`git diff --check` 和敏感信息
- 使用显式路径 staging；不得使用会混入无关变化的全量 staging
- 不提交 `.env`、密钥、本地数据库、日志、真实照片、健康数据或临时运行产物
- 依赖、锁文件和生成文件只有在 Task 必需且已审查时才能提交；工具副作用必须单独报告
- 不使用 `git reset --hard`、`git clean` 或覆盖他人改动
- 治理、产品规则和安全边界变更应使用可独立审查的 commit

`opencode.json` 提供最低机械权限边界；文本规则不能替代代码校验、测试、CI、权限和运行时安全门。

## 7. 健康、AI 与隐私硬边界

- 风险、禁忌、资格、训练量、营养边界和红旗规则由确定性代码或版本化策略数据实现
- 引入健康阈值或安全规则前核对当前权威一手来源，并记录来源、适用范围和版本
- AI 不能成为安全关键判断的唯一来源；输出必须经过类型 schema 和确定性校验
- AI 失败、超时、解析失败、缺失字段或无效输出不得转换成“正常”或健康结论
- 红旗阻断相关生成、替换、调整和产品内执行路径；受限用户不能进入普通自动计划路径
- 每次生成、替换或调整前使用最新结构化数据重新执行风险分类或安全门
- 测试和评测始终只使用合成数据
- 真实健康数据、照片、症状和饮食限制不得进入代码 Agent、测试夹具、日志、截图、Issue 或外部工具，除非存在单独批准的受控流程

这里的“产品运行时 Agent”与“开发 Agent”是不同角色。产品运行时 Agent 只能调用受控应用服务，不得任意执行 SQL、直接把自然语言写入正式档案、把 Prompt 当作唯一规则来源，或把聊天记录当作长期事实。

## 8. 验证与审查

Finding 严重级别：P0 为必须立即停止的安全或数据风险；P1 为阻止提交的高风险正确性问题；P2 为提交前必须修复的普通正确性、契约或测试缺口；P3 为不阻止提交的改进建议。只有用户可以明确接受 P1/P2 残余风险，自动提交不得自行豁免。

- 后端：相关 pytest；共享行为或发布关键变更还需完整测试
- Flutter：相关 test 和 `flutter analyze`；用户流程按风险做目标平台冒烟
- API：OpenAPI、序列化、鉴权、无效输入和服务失败
- 迁移：upgrade/downgrade、约束和恢复策略；需要时在可丢弃 PostgreSQL 演练
- 安全/AI：失败路径、无效输出、红旗、受限、缺失数据、越权和 prompt injection
- 隐私/鉴权/健康策略/迁移/产品运行时 Agent 权限属于高风险变更，需要独立二次审查和回滚说明

验证证据必须记录命令、工作目录、SHA、退出状态和关键计数。验证后发生实质改动，相关证据即失效。

## 9. 文档与冲突

协调者负责保持 `docs/agent/ACTIVE_TASKS.md`、当前规格、计划和路线图状态一致。未验证功能不得标记完成。

“应该怎样”由当前安全边界、已批准产品文档、ADR、规格和计划决定；“现在实际怎样”由运行代码、schema、迁移、测试和运行结果证明。现有代码或绿色测试不能覆盖规范要求。

平台的系统/开发者指令和用户在当前会话中的明确决定优先于仓库指导，但实现 Agent 不得把临时对话当作绕过健康、安全或隐私边界的许可。此类范围变化必须交由协调者确认，并先更新相应规范再实现。
